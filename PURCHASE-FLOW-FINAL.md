# NEXIVO HUB Purchase Flow

## Discord purchase flow
1. Discord에서 `/자판기` 실행
2. `구매` → 카테고리 → 상품 선택
3. `구매하기` → 수량 입력(1~99)
4. 잔액이 부족하면 구매 실패 안내
5. 잔액이 충분하면 `잠시만 기다려주세요...` 표시
6. 서버에서 잔액 차감 → 상품 지급/라이선스 생성
7. 1회용 웹 발급 링크 생성
8. Discord에 웹 발급 링크 버튼 표시 및 가능하면 구매자 DM 전송
9. 링크에서 라이선스 키 확인
10. Discord에서 `/라이센스 <라이선스코드>`로 활성화
11. 라이선스는 구매한 Discord User ID에 귀속되어 다른 사람의 사용을 거부

## One-time delivery link
- 발급 링크는 랜덤 토큰의 SHA-256 해시만 DB에 저장합니다.
- 링크를 한 번 사용하면 재사용할 수 없습니다.
- 라이선스 키 자체도 구매자의 Discord User ID와 묶여 있어 다른 사용자가 `/라이센스`로 활성화할 수 없습니다.
- 웹사이트의 `/claim/<token>` 페이지는 키를 직접 URL에 넣지 않습니다.

## Wallet
- 자동결제/자동충전은 사용하지 않습니다.
- Discord에서 계좌이체 충전 신청 → 운영자 승인 → 지갑 반영 구조입니다.
